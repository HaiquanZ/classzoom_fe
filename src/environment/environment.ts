export const environment = {
    server: {
        apiUrl: 'http://localhost:4000/api/v1',
    },
    path: {
        auth: {
            LOGIN: '/user/login',
            REGISTER: '/user/register'
        }
    }
};
